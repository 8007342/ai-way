# Generated files
modelfiles/

# Build logs (local development)
build-log-*.txt

# Yollayah runtime state - now in workdir/
# See workdir/README.md for details
workdir/

# Legacy state files (pre-modular) - clean up if found
.state/
.user/
.logs/
.yollayah-state
.yollayah-settings

# Cloned agents repo (the breadcrumb to YOU.md)
# Note: yollayah/lib/agents/ is tracked - only the root agents/ is ignored
agents/
!yollayah/lib/agents/

# Rust build artifacts (all workspace targets)
target/
**/target/

# Rust compiled artifacts
**/*.rlib
**/*.rmeta
**/*.d
**/*.so
**/*.dylib
**/*.dll
**/*.a
**/*.lib

# Rust incremental compilation
**/incremental/
**/.fingerprint/
**/deps/
**/build/
**/examples/*.pdb

# Cargo lock files (libraries don't commit these)
# NOTE: Binary projects SHOULD commit Cargo.lock
**/Cargo.lock
Cargo.lock

# Cargo output
**/Cargo.toml.orig

# Docker volumes (persistent data)
models/
tensors/
data/

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db
